/**
 * @fileoverview Error boundary components for graceful error handling throughout
 * the application, preventing crashes and providing user-friendly fallbacks.
 */

export { default as ErrorBoundary } from './ErrorBoundary'
export { default as SectionErrorBoundary } from './SectionErrorBoundary'
